# Database Configuration
DATABASE_URL=postgresql://username:password@your-neon-host.neon.tech/todo-evolution?sslmode=require

# Better Auth Configuration
BETTER_AUTH_SECRET=your-generated-secret-here  # Generate with: openssl rand -base64 32
JWT_SECRET=your-generated-jwt-secret-here      # Generate with: openssl rand -base64 32

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Environment
ENVIRONMENT=development  # development, staging, production
