# ============================================================
# Todo Evolution - Environment Configuration
# ============================================================
#
# Phase I (Current): SQLite - No environment variables needed
#   - Database: todo.db (auto-created in working directory)
#   - User ID: Via --user flag (no authentication)
#
# Phase II (Future): PostgreSQL migration
#   - Uncomment DATABASE_URL below when migrating to PostgreSQL
#   - Requires python-dotenv package: pip install python-dotenv
# ============================================================

# Phase II: PostgreSQL Database URL (currently unused)
# DATABASE_URL=postgresql://user:password@localhost:5432/todo_evolution

# Phase II: Optional Configuration
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10
# LOG_LEVEL=INFO
